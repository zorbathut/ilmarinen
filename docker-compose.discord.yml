services:
  discord-bot:
    build:
      context: .
      dockerfile: src/Ilmarinen.DiscordBot/Dockerfile
    environment:
      ILMARINEN_SERVER_URL: http://server:8080
    volumes:
      - ./config:/app/config:ro
