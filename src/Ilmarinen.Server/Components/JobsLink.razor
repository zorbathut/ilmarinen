
@if (JobIds != null && JobIds.Count > 0)
{
    @foreach (var jobId in JobIds)
    {
        <a href="/jobs/@jobId">@jobId.ToString()[..8]</a>
    }
}
else
{
    <span>-</span>
}

@code {
    [Parameter]
    public IReadOnlyList<Guid>? JobIds { get; set; }
}
